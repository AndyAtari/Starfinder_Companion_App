<%= form_for(@starship) do |f|%>
    
  <%= render partial: 'form', locals: {f: f}%>

<%end%>

<%= button_to("Delete Starship", @starship, {:onclick => "return confirm('Are you sure?')", :method => :delete, :remote => true}) %>